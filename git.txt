git

cd 203101-mishka перейти в папку
git branch показать ветку
git checkout module-task1 перейти на другую ветку
git checkout -b module2-task1 создать ветку 

git add index.html
git commit -m "разметка"  сделать коммит

git push origin module2-task1

git checkout master

git pull academy master

git push origin master

git merge module2-task1


